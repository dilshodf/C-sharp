// Показать числа от -N до N

int af = -5;
int uf = 5;
System.Console.WriteLine($"{af} .. {uf}"); // указали то, что и просили

// Что в этом коде можно улучшить?
// 1. Нужно точно менять имя переменных
// 2. Давать имена методам которые отражает суть того, что делают
// 3. Именовать аргументы метода

// Оператор bool
// Не сипользуйте для проверки вида b == false
// Вместо этого используйте !b 
// !b - если b - ложь, значит !b (НЕ b) - истина